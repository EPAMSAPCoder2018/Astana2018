context core {
   entity test {
        key Id   : Integer generated by default as identity(start with 1 increment by 1 no minvalue maxvalue 100 no cache no cycle);
            orderDate : UTCTimestamp;
    }
    technical configuration {
        column store;
    };
   
};